"""Dependency specific initialization."""

def deps(repo_mapping = {}):
    # We use '_' variable to avoid buildifier's warning
    # about using unused variable 'repo_mapping'.
    _ = repo_mapping
    pass
